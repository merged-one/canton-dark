# Infra

Infrastructure assets, deployment manifests, and runtime overlays should land here when they become concrete. Keep operator-owned policy in headless packages and treat infrastructure as an execution layer, not a policy layer.
